services:
  - type: web
    name: app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .

databases:
  - type: database
    name: db
    plan: free
    image: postgres:13.1
    envVars:
      - key: SECRET_KEY
        sync: false
